import { PdfManagerRepository } from './pdf-manager-repository';

describe('PdfManagerRepository', () => {
  it('should be defined', () => {
    expect(new PdfManagerRepository()).toBeDefined();
  });
});
