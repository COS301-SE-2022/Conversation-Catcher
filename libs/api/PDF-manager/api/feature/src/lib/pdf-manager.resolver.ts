//import { Resolver, Query, Args } from '@nestjs/graphql';

//@Resolver((of) => PdfManagerEntity)
export class PdfManagerResolver {
    //constructor(private companyService: CompanyExploreService) {}
}
